name: Code quality CI

on:
  pull_request:
  push:
    branches:
      - main

# Superseded pushes to the same PR cancel their in-flight run; pushes to main
# never cancel so every mainline commit keeps a complete CI record.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master
        with:
          toolchain: "1.95.0"
          components: rustfmt

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master
        with:
          toolchain: "1.95.0"
          components: clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: test

      - name: Run clippy
        run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings

  test:
    runs-on: ubuntu-latest
    # Real peer attestation hashes the test executable on each connection.
    # Keep debug sections from dominating those bounded IPC checks, matching
    # the existing native acceptance and coverage build profiles.
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        # The action exports RUSTUP_TOOLCHAIN, which outranks rust-toolchain.toml,
        # so the version here must match the pin in rust-toolchain.toml.
        uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master
        with:
          toolchain: "1.95.0"
          components: clippy, rustfmt

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: test

      - name: Install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: nextest

      - name: Run workspace tests
        run: cargo nextest run --locked --workspace --all-features

      - name: Run documentation tests
        # nextest does not execute doctests; keep them covered separately.
        run: cargo test --locked --doc --workspace --all-features

      - name: Build independent core consumer
        run: cargo run --locked --manifest-path examples/embedded-policy/Cargo.toml

      - name: Verify wrapper enforcement handshake test
        # --exact needs the full module path; a bare name matches 0 tests and
        # passes vacuously. The grep guard fails the step if the test ever
        # stops running (renamed/moved), instead of silently passing.
        run: |
          cargo test --locked -p opaqued tests::enforce_agent_sessions_rejects_unwrapped_and_allows_session_token -- --exact 2>&1 | tee /tmp/wrapper-test.out
          grep -q "test result: ok. 1 passed" /tmp/wrapper-test.out

  unattended-acceptance:
    name: Declared unattended protocol acceptance
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Verify strict runner regressions
        run: python3 -B -m unittest discover -s scripts -p test_synthesized_suite.py -v
      - name: Run exact declared tests with real split-UID prerequisites
        # The composed tests are ignored in ordinary nonroot workspace
        # runs. The manifest explicitly executes them with zero ignored tests.
        # It never invokes the native approval UI.
        run: |
          sudo -n env \
            PATH="$PATH" \
            CARGO_HOME="$HOME/.cargo" \
            RUSTUP_HOME="$HOME/.rustup" \
            RUSTUP_TOOLCHAIN=1.95.0 \
            python3 -B scripts/synthesized_suite.py \
              --profile protocol \
              --target-dir "$GITHUB_WORKSPACE/target" \
              --output "$RUNNER_TEMP/synthesized-protocol"
      - name: Restore report and build-cache ownership
        if: always()
        run: |
          # Root compiles the split-UID fixtures. Return generated directories
          # to the runner before artifact upload and rust-cache's post step.
          for generated_path in \
            "$RUNNER_TEMP/synthesized-protocol" \
            "$GITHUB_WORKSPACE/target" \
            "$HOME/.cargo/registry" \
            "$HOME/.cargo/git"; do
            if [ -d "$generated_path" ]; then
              sudo -n chown -R --no-dereference "$(id -u):$(id -g)" "$generated_path"
            fi
          done
      - name: Retain scoped acceptance results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: declared-protocol-acceptance
          path: |
            ${{ runner.temp }}/synthesized-protocol/report.json
            ${{ runner.temp }}/synthesized-protocol/junit.xml
          if-no-files-found: error

  measured-code-coverage:
    name: Workspace production coverage (${{ matrix.platform }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            runner: ubuntu-24.04
            python-version: "3.12"
          - platform: darwin
            runner: macos-15
            python-version: "3.12.10"
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    env:
      # This explicit selection outranks the repository's stable product pin.
      RUSTUP_TOOLCHAIN: nightly-2026-09-13
    steps:
      - uses: actions/checkout@v6
        with:
          # Resolve the immutable PR base / push-before object and historical
          # baseline source identities; an unavailable object must fail closed.
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0
        with:
          toolchain: nightly-2026-09-13
          components: llvm-tools-preview
      - if: matrix.platform == 'darwin'
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> ${{ runner.temp }}/critical-coverage-target"
          key: critical-coverage-${{ matrix.platform }}
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Verify collector and measured-coverage regressions
        run: |
          python3 -B -m unittest discover -s scripts -p 'test_*coverage*.py' -v
          for directory in tests/packaged tests/real-model tests/browser; do
            python3 -B -m unittest discover -s "$directory" -p 'test_*.py' -v
          done
      - name: Install pinned coverage collector
        if: matrix.platform == 'darwin'
        run: cargo install cargo-llvm-cov --version 0.9.1 --locked
      - if: matrix.platform == 'darwin'
        uses: actions/setup-node@v6
        with:
          node-version: "22"
      - name: Verify disposable host runner regressions
        if: matrix.platform == 'linux'
        run: |
          python3 -m pip install --disable-pip-version-check -r scripts/requirements-test.txt
          python3 -B -m unittest discover -s tests/contained-ssh -p 'test_*.py' -v
      - name: Collect every workspace package and instrumented subprocess
        # A killed-child preflight verifies continuous branch profiles before
        # any measurements. The same instrumentation builds test, daemon and
        # adapter binaries; split-UID peer objects/profiles are retained too.
        # Linux runs real SSH and controlled inference RPC scenarios inside a
        # disposable systemd/Vault/OpenSSH host. Cargo metadata defines the
        # full workspace scope, including non-default members and binaries.
        # The original three-package subset is only a historical comparison.
        run: |
          if [ "${{ matrix.platform }}" = linux ]; then
            coverage_status=0
            python3 -B tests/contained-ssh/run.py --coverage \
              --acceptance packaged --acceptance browser --acceptance model --acceptance service \
              --output "$RUNNER_TEMP/critical-contained" || coverage_status=$?
            if [ -d "$RUNNER_TEMP/critical-contained/container/coverage" ]; then
              sudo -n chown -R --no-dereference "$(id -u):$(id -g)" \
                "$RUNNER_TEMP/critical-contained/container/coverage"
              mv "$RUNNER_TEMP/critical-contained/container/coverage" \
                "$RUNNER_TEMP/critical-coverage"
            fi
            exit "$coverage_status"
          else
            python3 -B scripts/collect_critical_coverage.py \
              --source-root "$GITHUB_WORKSPACE" \
              --target-dir "$RUNNER_TEMP/critical-coverage-target" \
              --reuse-target-dir \
              --acceptance packaged --acceptance browser \
              --output "$RUNNER_TEMP/critical-coverage"
          fi
      - name: Enforce per-crate tiers and native-target coverage ratchet
        # Every tier is mandatory now. A different OS or architecture cannot
        # qualify this target, and a missing baseline is a failure. Event SHAs
        # are data, validated as complete immutable commits by the gate.
        env:
          COVERAGE_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          coverage_target=$(rustc -vV | sed -n 's/^host: //p')
          python3 -B scripts/check_coverage_policy.py \
            --report "$RUNNER_TEMP/critical-coverage/llvm-coverage.json" \
            --collection "$RUNNER_TEMP/critical-coverage/collection.json" \
            --source-root "$GITHUB_WORKSPACE" \
            --target "$coverage_target" \
            --policy config/coverage-policy.json \
            --baseline "config/coverage-baselines/$coverage_target.json" \
            --baseline-reference "$COVERAGE_BASE_SHA" \
            --allow-baseline-bootstrap \
            --output "$RUNNER_TEMP/critical-coverage/gate.json"
      - name: Restore generated artifact and cache ownership
        if: always() && matrix.platform == 'linux'
        run: |
          for generated_path in \
            "$RUNNER_TEMP/critical-coverage" \
            "$RUNNER_TEMP/critical-contained" \
            "$RUNNER_TEMP/critical-coverage-target" \
            "$HOME/.cargo/registry" \
            "$HOME/.cargo/git"; do
            if [ -d "$generated_path" ]; then
              sudo -n chown -R --no-dereference "$(id -u):$(id -g)" "$generated_path"
            fi
          done
      - name: Retain platform-specific collection inventory and coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: workspace-production-coverage-${{ matrix.platform }}
          path: |
            ${{ runner.temp }}/critical-contained/run.json
            ${{ runner.temp }}/critical-coverage/collection.json
            ${{ runner.temp }}/critical-coverage/coverage-summary.json
            ${{ runner.temp }}/critical-coverage/original-scope-summary.json
            ${{ runner.temp }}/critical-coverage/baseline-summary.json
            ${{ runner.temp }}/critical-coverage/baseline-llvm-coverage.json
            ${{ runner.temp }}/critical-coverage/gate.json
            ${{ runner.temp }}/critical-coverage/llvm-coverage.json
            ${{ runner.temp }}/critical-coverage/acceptance-*/suite/report.json
            ${{ runner.temp }}/critical-coverage/acceptance-model/model-service.json
          if-no-files-found: error

  test-macos:
    name: Native macOS tests
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test
      - name: Install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: nextest
      - name: Run native workspace tests
        run: cargo nextest run --locked --workspace --all-features
      - name: Run documentation tests
        # nextest does not execute doctests; keep them covered separately.
        run: cargo test --locked --doc --workspace --all-features
      - name: Verify native reviewer package without installing
        run: |
          cargo build --locked -p opaque-approver -p opaque-approve-helper
          reviewer_pkg_dir=$(mktemp -d "${RUNNER_TEMP}/opaque-reviewer.XXXXXX")
          trap 'rm -rf "$reviewer_pkg_dir"' EXIT
          python3 scripts/package-reviewer-macos.py --binary-dir target/debug --output "$reviewer_pkg_dir/OpaqueReviewer.app" --architecture "$(uname -m)" --ad-hoc
          "$reviewer_pkg_dir/OpaqueReviewer.app/Contents/MacOS/OpaqueReviewer" --self-test
          codesign --verify --deep --strict "$reviewer_pkg_dir/OpaqueReviewer.app"

  python:
    name: Python protocol, evidence and operator tests
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            scripts/requirements-test.txt
            docs/requirements.txt
      - name: Install pinned test dependencies
        run: python -m pip install --disable-pip-version-check -r scripts/requirements-test.txt
      - name: Run every Python regression without skips
        # Linux runs the real peer-credential/parent-death cases. Installing the
        # pinned FIDO2 library makes its API-contract cases mandatory as well.
        # The packaged SSH host's signed-control and health-contract tests are
        # a separate discovery root and must also run without skips.
        run: |
          # Independent processes prevent same-named test_runner modules in
          # separate roots from shadowing one another in sys.modules.
          for directory in scripts packaging/ssh-host/tests tests/contained-ssh tests/real-model tests/packaged; do
            python -B - "$directory" <<'PYTEST'
          import sys, unittest
          directory = sys.argv[1]
          suite = unittest.TestLoader().discover(directory, pattern="test_*.py")
          result = unittest.TextTestRunner(verbosity=2).run(suite)
          if not result.wasSuccessful() or not result.testsRun or result.skipped:
              print(f"Required suite {directory}: failures, missing tests, or skipped tests", file=sys.stderr)
              sys.exit(1)
          PYTEST
          done

  javascript:
    name: JavaScript privacy and DOM regressions
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "26.8.1"
      - name: Run all Node regression suites
        # The customer demo has its own repository. All three suites here are
        # required: fail glob expansion if any directory loses its test files.
        run: |
          shopt -s failglob
          node --test \
            deploy/cloudflare-docs-privacy/tests/*.test.mjs \
            crates/opaque-showcase/tests/*.test.cjs \
            crates/opaque-web/tests/*.test.cjs

  browser-acceptance:
    name: Real Chromium dashboard acceptance
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test
      - uses: actions/setup-node@v6
        with:
          node-version: "26.8.1"
          cache: npm
          cache-dependency-path: tests/browser/package-lock.json
      - name: Build the real dashboard and daemon
        env:
          # Keep executable identity hashing within the production IPC deadline;
          # DWARF symbols are not part of the browser acceptance behavior.
          CARGO_PROFILE_DEV_DEBUG: "0"
        run: cargo build --locked -p opaque-web -p opaqued
      - name: Install pinned Chromium test runtime
        working-directory: tests/browser
        run: |
          npm ci --ignore-scripts --no-audit --no-fund
          npx playwright install --with-deps chromium
      - name: Exercise owner authentication and real process boundaries
        working-directory: tests/browser
        run: npm test

  real-model-acceptance:
    name: Pinned model and signed review acceptance
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Verify owned model harness regressions
        run: python3 -B -m unittest discover -s tests/real-model -p 'test_*.py' -v
      - name: Run actual pinned CPU generation through the broker
        run: |
          python3 -B tests/contained-ssh/run.py --real-model \
            --image opaque-contained-model:ci --output "$RUNNER_TEMP/real-model"
      - name: Retain source-bound model qualification
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: real-model-acceptance
          path: |
            ${{ runner.temp }}/real-model/run.json
            ${{ runner.temp }}/real-model/container/model-service.json
            ${{ runner.temp }}/real-model/container/suite/report.json
            ${{ runner.temp }}/real-model/container/suite/junit.xml
          if-no-files-found: error

  packaged-acceptance:
    name: Installed native artifacts (${{ matrix.platform }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            runner: ubuntu-24.04
            python-version: "3.12"
          - platform: darwin
            runner: macos-15
            python-version: "3.12.10"
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Verify installed-artifact guard regressions
        run: python3 -B -m unittest discover -s tests/packaged -p 'test_*.py' -v
      - name: Build all real candidate tools
        run: cargo build --locked --workspace --bins
      - name: Package the exact native build
        # Candidate build bytes; published signing/notarization stays separate.
        run: |
          python3 -B tests/packaged/build.py --binary-dir target/debug \
            --output "$RUNNER_TEMP/packaged-candidate"
      - name: Exercise the installed tools and actual account custody
        run: |
          if [ "${{ matrix.platform }}" = linux ]; then
            sudo -n env PATH="$PATH" CARGO_HOME="$HOME/.cargo" RUSTUP_HOME="$HOME/.rustup" \
              python3 -B scripts/synthesized_suite.py --profile packaged \
                --packaged-archive "$RUNNER_TEMP/packaged-candidate/candidate.tar.gz" \
                --packaged-owner-user "$(id -un)" --packaged-peer-user nobody \
                --output "$RUNNER_TEMP/packaged-acceptance"
          else
            python3 -B scripts/synthesized_suite.py --profile packaged \
              --packaged-archive "$RUNNER_TEMP/packaged-candidate/candidate.tar.gz" \
              --output "$RUNNER_TEMP/packaged-acceptance"
          fi
      - name: Restore owned report access
        if: always() && matrix.platform == 'linux'
        run: |
          if [ -d "$RUNNER_TEMP/packaged-acceptance" ]; then
            sudo -n chown -R --no-dereference "$(id -u):$(id -g)" "$RUNNER_TEMP/packaged-acceptance"
          fi
      - name: Retain native installed-artifact qualification
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: installed-artifacts-${{ matrix.platform }}
          path: |
            ${{ runner.temp }}/packaged-acceptance/report.json
            ${{ runner.temp }}/packaged-acceptance/junit.xml
          if-no-files-found: error

  site-privacy:
    name: Generated visitor site privacy
    # This is a read-only build gate in both repositories, including fork PRs.
    # Publication remains separately guarded in the deployment workflows.
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            scripts/requirements-test.txt
            docs/requirements.txt
      - name: Install pinned documentation tools
        run: python -m pip install --disable-pip-version-check -r scripts/requirements-test.txt
      - name: Check privacy gate regressions
        run: python -B -m unittest discover -s scripts -p test_check_site_privacy.py
      - name: Build and inspect all publication artifacts
        # The temporary source includes private sentinels. It and its generated
        # site are deliberately never uploaded as a preview or Actions artifact.
        run: python -B scripts/check_site_privacy.py --build

  # Verifies the Linux-only trust-domain guarantees (separate-uid custody,
  # Landlock, seccomp). The probe runs FIRST and fails the job if the runner
  # kernel can't enforce Landlock/seccomp — so the enforcement tests that later
  # steps run can never green-skip on an incapable kernel. Locally the same
  # entry points run via `scripts/linux-harness.sh` (Docker on macOS; note
  # Docker Desktop's linuxkit kernel lacks Landlock, so Landlock enforcement
  # is verified here and only here).
  trust-domain-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        # Keep in sync with rust-toolchain.toml (the action's RUSTUP_TOOLCHAIN
        # export outranks the file).
        uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master
        with:
          toolchain: "1.95.0"

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: test

      - name: Kernel capability probe (landlock + seccomp must be enforceable)
        run: ./scripts/linux-harness.sh probe

      - name: Federation e2e (signed bundles, chain-verifiable export, attestation)
        # Runs BEFORE the root-gated suites on purpose: those invoke cargo
        # under sudo and leave root-owned artifacts in target/, which a later
        # non-root cargo cannot lock. The grep fails the step if the suite
        # ever stops running (renamed/moved), instead of passing vacuously.
        run: |
          cargo test --locked -p opaqued --test federation_e2e 2>&1 | tee /tmp/fed-e2e.out
          grep -q "test result: ok. 3 passed" /tmp/fed-e2e.out

      - name: Provider e2e (canonical authority and real mock-provider effects)
        # Require original positive controls and the canonical-action regressions,
        # while allowing the suite to grow. Run before root-owned build artifacts.
        run: |
          set -o pipefail
          cargo test --locked -p opaqued --test provider_e2e 2>&1 | tee /tmp/prov-e2e.out
          grep -Eq "test result: ok\. [1-9][0-9]* passed; 0 failed" /tmp/prov-e2e.out
          for required in \
            github_set_actions_secret_succeeds_end_to_end \
            unlisted_provider_operation_is_policy_denied \
            canonical_action_rejects_target_substitution_before_approval_and_provider_io \
            canonical_action_derives_omitted_targets_for_policy_and_equivalent_dispatch \
            canonical_action_rejects_malformed_targets_and_unknown_provider_fields \
            canonical_action_policy_includes_implicit_token_refs_and_ignores_forged_ref_lists \
            canonical_action_policy_constrains_actual_secret_scope_and_environment \
            canonical_action_rejects_task_only_operations_under_allow_all_policy \
            canonical_reference_metadata_rejects_secret_bytes_before_audit_or_review; do
            grep -Fxq "test ${required} ... ok" /tmp/prov-e2e.out
          done

      - name: MCP broker e2e (signed tools, approval, durable charges, real fixture effects)
        run: |
          set -o pipefail
          cargo build --locked -p opaque-mcp --bin opaque-mcp
          cargo test --locked -p opaque-bounded-work mcp:: --lib
          cargo test --locked -p opaqued --bin opaqued mcp_approval_reviews
          cargo test --locked -p opaqued --test mcp_gateway_e2e 2>&1 | tee /tmp/mcp-e2e.out
          grep -Eq "test result: ok\. [1-9][0-9]* passed; 0 failed" /tmp/mcp-e2e.out
          for required in \
            adapter_signed_tool_daemon_effect_receipt_and_replay_survive_restart \
            policy_denial_malformed_input_and_generic_bypass_make_no_http_calls \
            revoke_during_handshake_stops_final_tool_dispatch_and_does_not_refund \
            daemon_death_after_effect_recovers_unknown_and_never_replays \
            expiry_during_handshake_prevents_tool_effect_and_preserves_charge; do
            grep -Fxq "test ${required} ... ok" /tmp/mcp-e2e.out
          done

      - name: Multi-uid isolation suite (custody + privilege drop, as root)
        run: ./scripts/linux-harness.sh isolation

      - name: Trust-domain split e2e (real daemon, two uids, signed approver)
        run: ./scripts/linux-harness.sh e2e-split

  supply-chain:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check supply chain
        uses: EmbarkStudios/cargo-deny-action@v2
